// Re-export from the atomic design library location.
// Keep this file so existing imports (`@/components/Button`) continue to work.
export { default } from "@/components/atoms/Button/Button";
export type { ButtonProps, ButtonVariant, ButtonSize } from "@/components/atoms/Button/Button";
